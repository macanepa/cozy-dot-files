# Python bytecode (qtile compiles configs at runtime)
__pycache__/
*.pyc

# Tool-generated backups
*.bak

# Machine-local shell overrides (never commit machine/work-specific config)
.zshrc.local

# Runtime theme selection (per-machine; defaults to green if absent)
.config/qtile/.current_theme
